import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  label: {
    textAlign: 'left',
  },
  dropdown: {
    padding: 0,
    width: 250,
    marginTop: 8,
    marginLeft: 100,
    borderRadius: 4,
    zIndex: 10,
  },
  item: {
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
});
