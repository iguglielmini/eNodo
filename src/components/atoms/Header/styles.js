import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 32,
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
